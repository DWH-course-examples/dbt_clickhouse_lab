
version: 2

models:
  - name: dds_order_fact
    description: "фактовыя табличка с заказами"
    columns:
      - name: agentId
        description: "наименование агента источника"
      - name: trackingPointId
        description: "наименование агента источника"
    materialized: table

